SUPABASE_URL=###
SUPABASE_KEY=###
AUTHORIZED_USER_ID=###
OPENAI_API_KEY=###

# Placeholder only. Do not store real provider keys in committed files.
# For local development, provide OPENAI_API_KEY through the shell or OS environment.
# Production Workers must configure OPENAI_API_KEY manually through Cloudflare dashboard
# or Wrangler secrets before deployed diagnosis smoke tests can pass.
