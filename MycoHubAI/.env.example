SUPABASE_URL=###
SUPABASE_KEY=###
AUTHORIZED_USER_ID=###
OPENROUTER_API_KEY=###

# Placeholder only. Do not store real provider keys in committed files.
# For local development, provide OPENROUTER_API_KEY through the shell, OS environment,
# or uncommitted .dev.vars.
# Production Workers must configure OPENROUTER_API_KEY manually through Cloudflare dashboard
# or Wrangler secrets before deployed diagnosis smoke tests can pass.
