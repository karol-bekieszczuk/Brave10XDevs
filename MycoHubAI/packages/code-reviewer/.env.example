# Export these variables in your shell or process environment. Never commit real keys.
OPENROUTER_API_KEY=
# The model is configurable so callers can select any OpenRouter-supported model.
OPENROUTER_MODEL=openai/gpt-4o
